{
  "devDependencies": {
    "@parcel/transformer-sass": "^2.8.3",
    "cssnano": "^4.1.10",
    "parcel": "^2.8.3",
    "sass": "^1.26.9"
  },
  "scripts": {
    "serve": "rm -rf .parcel-cache/ dist/ && parcel src/index.html",
    "build": "rm -rf .parcel-cache/ dist/ && parcel build src/index.html --public-url ./"
  }
}
